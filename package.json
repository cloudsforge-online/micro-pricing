{
  "name": "@cloudsforge/pricing",
  "version": "2026.08.47",
  "private": true,
  "description": "The price oracle: four independent sources, a median in BigInt fixed point, divergence rejection, fail-closed staleness, and administered prices for assets with no market. Quotes live in a table, not in a per-replica Map.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --import tsx src/index.ts",
    "migrate": "node --import tsx src/migrator.ts",
    "typecheck": "tsc --noEmit",
    "_test": "--test-concurrency=1 is required, not a preference: every database test file truncates between cases, and node:test runs FILES in parallel by default. A TRUNCATE takes an AccessExclusiveLock, so one file's reset deadlocks against another file's inserts (40P01). The ledger learned this the hard way. Cases within a file already run in order.",
    "test": "node --import tsx --test --test-concurrency=1 src/*.test.ts",
    "build": "tsc -p tsconfig.build.json",
    "check": "pnpm typecheck && pnpm test"
  },
  "_localDependencies": [
    "The @cloudsforge/* packages are consumed from sibling checkouts because none of them is",
    "published yet. Once AD-02 lands they all become registry versions ('^1.0.0') and these paths —",
    "plus the two build contexts in the Dockerfile — are deleted. Nothing else changes.",
    "",
    "The runtime packages take 'file:' and the contracts packages take 'link:'. The difference is",
    "not cosmetic: a contracts package depends on its siblings with the 'workspace:^' protocol,",
    "which only resolves inside the contracts workspace. 'link:' symlinks the package in place and",
    "lets it keep using its own already-installed node_modules.",
    "",
    "tsx is a runtime dependency for the same reason: the unpublished packages resolve to their",
    "TypeScript sources."
  ],
  "dependencies": {
    "@cloudsforge/auth": "file:../runtime/packages/auth",
    "@cloudsforge/contracts-chain": "link:../contracts/packages/chain",
    "@cloudsforge/contracts-events": "link:../contracts/packages/events",
    "@cloudsforge/db": "file:../runtime/packages/db",
    "@cloudsforge/http": "file:../runtime/packages/http",
    "@cloudsforge/jobs": "file:../runtime/packages/jobs",
    "@cloudsforge/lifecycle": "file:../runtime/packages/lifecycle",
    "@cloudsforge/secrets": "file:../runtime/packages/secrets",
    "@cloudsforge/telemetry": "file:../runtime/packages/telemetry",
    "postgres": "^3.4.5",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "jose": "^5.9.6",
    "typescript": "^5.7.2"
  }
}
